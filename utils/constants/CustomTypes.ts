export enum UserEligibility {
  PENDING = "PENDING",
  ELIGIBLE = "ELIGIBLE",
  NOT_ELIGIBLE = "NOT_ELIGIBLE",
}

export enum ClaimStatus {
  PENDING = "PENDING",
  SUCCESS = "SUCCESS",
  FAIL = "FAIlED",
  NOT_STARTED = "NOT_STARTED",
}
